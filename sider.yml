# Customize each tool.
linter:
  # ESLint
  eslint:
    config: .eslintrc.json
    ignore-path: .eslintignore

# Ignore specific files.
ignore:
  - "*.pdf"
  - "*.mp4"
  - "*.min.*"

# Exclude specific branches.
branches:
  exclude:
    - "/^dependabot/"
    
