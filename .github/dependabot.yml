version: 2
updates:
  # Bun has its own ecosystem; `npm` would not maintain `bun.lock`, and a manifest-only
  # update would then fail every `--frozen-lockfile` install.
  - package-ecosystem: bun
    directory: "/"
    schedule:
      interval: weekly
